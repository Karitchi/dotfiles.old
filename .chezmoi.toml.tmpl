{{- $textEditorList := list "nano" "micro" "nvim" "code" -}}
{{- $textEditor := promptChoiceOnce . "textEditor" "What type of host are you on" $textEditorList -}}

[data]
	textEditor = {{- $textEditor -}}

[git]
	autoCommit = true
	autoPush = true

